// 함수의 return

/* 문제 1 : 두 개의 숫자를 매개변수로 받아 그 합을 반환하는 함수 'addNumbers'를 작성하세요. */

function addNumbers(a, b) {
    return a + b;
}

const result1 = addNumbers(5, 3);
const result2 = addNumbers(-1, 7);
console.log(result1 + result2); // 14를 출력해야 합니다.
